import { createBrowserHistory } from 'history'; // we're going to set up a history object and this is done with createBrowserHistory. The history object is used under the hood by React to keep track of the pages we've visited throughout our application




export default createBrowserHistory();